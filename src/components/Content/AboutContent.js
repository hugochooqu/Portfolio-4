import classes from "./AboutContent.module.css";

const About = () => {
  return (
    <section className={classes.about}>
        <p>hello</p>
     
    </section>
  );
};

export default About;
